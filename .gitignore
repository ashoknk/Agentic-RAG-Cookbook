

# Ignore all hidden files and folders (those starting with a dot)
.*
.env
.venv
dotenv.txt

# Ignore all vector databases generated 
faiss_*
chroma_db*

# Optional: If you want to make sure your .gitignore file itself is NOT ignored
!.gitignore

uv.lock 

